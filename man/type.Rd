% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/type.R
\name{type}
\alias{type}
\alias{int}
\alias{dbl}
\alias{chr}
\alias{lgl}
\alias{cpl}
\alias{raw}
\alias{lst}
\alias{na_ok}
\alias{opt}
\alias{nonneg}
\alias{between}
\alias{matches}
\alias{unique_vals}
\alias{nchar_between}
\title{The blaze type vocabulary}
\usage{
type(expr)
}
\arguments{
\item{expr}{A type expression such as \code{int() |> between(0, 1)}.}
}
\value{
A \code{blaze_type}.
}
\description{
blaze types are written with a small vocabulary that is available inside
\code{\link[=fn]{fn()}}, \code{\link[=check]{check()}}, and \code{type()} — never as top-level functions, so the terse
names never clash with base R or other packages. \code{type()} evaluates a type
expression and returns the resulting \code{blaze_type}, for building a reusable
type outside those contexts.
}
\details{
Base types describe a vector of one base type, named as \code{\link[=typeof]{typeof()}} reports it:
\code{int()}, \code{dbl()}, \code{chr()}, \code{lgl()}, \code{cpl()}, \code{raw()}, \code{lst()}. Each accepts
\code{len} for an exact length or \code{min}/\code{max} for inclusive length bounds.

Modifiers relax the defaults: \code{na_ok()} permits \code{NA}, \code{opt()} permits \code{NULL}.

Refinements add element-wise constraints, composed with the pipe: \code{nonneg()},
\code{between(min, max)}, \code{matches(pattern)}, \code{unique_vals()},
\code{nchar_between(min, max)}.

Use \code{\link[=blaze_types]{blaze_types()}} to list the vocabulary.
}
\examples{
pos_int <- type(int() |> nonneg())
check(5L, int(1))
}
